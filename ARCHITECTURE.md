# 🎯 HireLens AI - Visual System Architecture

## System Components Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                           USER INTERFACE                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────┐  │
│  │   Landing    │  │     Auth     │  │      Dashboard           │  │
│  │     Page     │  │  Login/Reg   │  │  - Upload Resume         │  │
│  │              │  │              │  │  - View ATS Score        │  │
│  │  index.html  │  │ login.html   │  │  - Match Jobs            │  │
│  │              │  │ register.html│  │  - See AI Feedback       │  │
│  └──────────────┘  └──────────────┘  │  dashboard.html          │  │
│                                       └──────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                    ┌────────┴────────┐
                    │  Fetch API      │
                    │  (JavaScript)   │
                    └────────┬────────┘
                             │
                        JSON/HTTP
                             │
┌────────────────────────────┴────────────────────────────────────────┐
│                        FASTAPI BACKEND                               │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │                    API ENDPOINTS                            │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │    │
│  │  │  /api/   │  │  /api/   │  │  /api/   │  │  /api/   │   │    │
│  │  │ register │  │ resume/  │  │  jobs/   │  │dashboard │   │    │
│  │  │  /login  │  │  upload  │  │  match   │  │          │   │    │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │    │
│  └───────────────────────┬────────────────────────────────────┘    │
│                          │                                          │
│  ┌───────────────────────┴────────────────────────────────────┐    │
│  │                BUSINESS LOGIC LAYER                         │    │
│  │                                                              │    │
│  │  ┌──────────────────┐     ┌─────────────────────────────┐  │    │
│  │  │  Authentication  │     │   Resume Processing         │  │    │
│  │  │  - JWT Tokens    │     │   - PDF Parser              │  │    │
│  │  │  - Password Hash │     │   - Text Cleaner            │  │    │
│  │  │  - User Session  │     │   - Skill Extractor (NLP)   │  │    │
│  │  └──────────────────┘     │   - ATS Scorer              │  │    │
│  │                            └─────────────────────────────┘  │    │
│  │                                                              │    │
│  │  ┌──────────────────────────┐   ┌──────────────────────┐   │    │
│  │  │   Job Matching Engine    │   │   AI Integration     │   │    │
│  │  │   - TF-IDF Vectorizer    │   │   - OpenAI GPT      │   │    │
│  │  │   - Cosine Similarity    │   │   - Google Gemini   │   │    │
│  │  │   - Skill Comparison     │   │   - Prompt Engine   │   │    │
│  │  └──────────────────────────┘   └──────────────────────┘   │    │
│  └──────────────────────────────────────────────────────────────    │
│                          │                                          │
│  ┌───────────────────────┴────────────────────────────────────┐    │
│  │                 DATA ACCESS LAYER                           │    │
│  │                                                              │    │
│  │  ┌────────────────────────────────────────────────────┐    │    │
│  │  │            SQLAlchemy ORM                           │    │    │
│  │  │  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐   │    │    │
│  │  │  │ Users  │  │Resumes │  │  Jobs  │  │Matches │   │    │    │
│  │  │  │ Table  │  │ Table  │  │ Table  │  │ Table  │   │    │    │
│  │  │  └────────┘  └────────┘  └────────┘  └────────┘   │    │    │
│  │  └────────────────────────────────────────────────────┘    │    │
│  └──────────────────────────────────────────────────────────────    │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                    ┌────────┴────────┐
                    │   SQLite DB     │
                    │  hirelens.db    │
                    └─────────────────┘

                External Services:
            ┌──────────────┐  ┌──────────────┐
            │  OpenAI API  │  │  Gemini API  │
            │ (Optional)   │  │  (Optional)  │
            └──────────────┘  └──────────────┘
```

---

## Data Flow: Resume Upload to Analysis

```
┌──────────────────────────────────────────────────────────────────────┐
│                        RESUME UPLOAD FLOW                            │
└──────────────────────────────────────────────────────────────────────┘

1. User Action
   │
   ├─► User clicks "Choose PDF File"
   │
   ├─► Selects resume.pdf
   │
   └─► Clicks "Upload & Analyze"

2. Frontend Processing (dashboard.js)
   │
   ├─► Validate file type (PDF only)
   │
   ├─► Create FormData object
   │
   ├─► Show loading animation
   │
   └─► Send POST /api/resume/upload

3. Backend Receives (resume.py)
   │
   ├─► Verify authentication token
   │
   ├─► Validate file format
   │
   └─► Read file bytes

4. PDF Processing (pdf_parser.py)
   │
   ├─► Try pdfplumber extraction
   │   ├─► Success → Continue
   │   └─► Fail → Try PyMuPDF
   │
   ├─► Clean extracted text
   │   ├─► Remove extra whitespace
   │   ├─► Normalize characters
   │   └─► Remove special chars
   │
   └─► Return cleaned text

5. NLP Analysis (skill_extractor.py)
   │
   ├─► Load spaCy model
   │
   ├─► Extract Technical Skills
   │   └─► Match: Python, JavaScript, React, etc.
   │
   ├─► Extract Soft Skills
   │   └─► Match: Leadership, Communication, etc.
   │
   ├─► Extract Tools
   │   └─► Match: Git, Docker, VS Code, etc.
   │
   ├─► Extract Keywords
   │   └─► NLP tokenization & POS tagging
   │
   └─► Count Action Verbs
       └─► Match: Developed, Led, Implemented, etc.

6. ATS Scoring (ats_scorer.py)
   │
   ├─► Calculate Keyword Density (40%)
   │   ├─► Count important sections
   │   ├─► Measure keyword frequency
   │   └─► Check technical skills
   │
   ├─► Calculate Formatting Score (20%)
   │   ├─► Verify email present
   │   ├─► Check phone number
   │   ├─► Validate dates
   │   ├─► Check bullet points
   │   └─► Verify word count (400-2000)
   │
   ├─► Calculate Action Verbs Score (15%)
   │   └─► Count strong verbs / 15 * 100
   │
   ├─► Calculate Experience Score (15%)
   │   ├─► Find quantifiable metrics
   │   └─► Check experience keywords
   │
   ├─► Calculate Skill Match (10%)
   │   └─► Match with job description
   │
   └─► Compute Weighted Final Score
       └─► (score1*0.4 + score2*0.2 + score3*0.15 + ...)

7. Database Storage (models.py)
   │
   ├─► Create Resume object
   │   ├─► user_id
   │   ├─► filename
   │   ├─► raw_text
   │   ├─► extracted_skills (JSON)
   │   ├─► technical_skills (JSON)
   │   ├─► soft_skills (JSON)
   │   ├─► tools (JSON)
   │   ├─► ats_score
   │   └─► created_at
   │
   ├─► db.add(resume)
   │
   ├─► db.commit()
   │
   └─► db.refresh(resume)

8. Response to Frontend
   │
   └─► Return JSON:
       {
         "id": 1,
         "filename": "resume.pdf",
         "ats_score": 85.5,
         "extracted_skills": [...],
         "technical_skills": [...],
         "soft_skills": [...],
         "tools": [...]
       }

9. Frontend Display (dashboard.js)
   │
   ├─► Update ATS score display
   │   └─► Create doughnut chart
   │
   ├─► Display extracted skills
   │   └─► Categorize by type
   │
   ├─► Create bar chart
   │   └─► Show skill distribution
   │
   ├─► Show score interpretation
   │
   └─► Update dashboard statistics

Total Time: 2-5 seconds
```

---

## Job Matching Flow

```
┌──────────────────────────────────────────────────────────────────────┐
│                      JOB MATCHING WORKFLOW                           │
└──────────────────────────────────────────────────────────────────────┘

1. User Input
   │
   ├─► Paste job description
   │
   ├─► (Optional) Enter job title
   │
   └─► Click "Calculate Match"

2. Backend Processing (jobs.py)
   │
   ├─► Get resume from database
   │
   ├─► Extract job skills
   │   └─► Use same NLP as resume
   │
   └─► Send to matcher

3. Matching Algorithm (matcher.py)
   │
   ├─► Extract Skills from Both
   │   ├─► Resume skills: {Python, JS, React}
   │   └─► Job skills: {Python, JS, AWS, Docker}
   │
   ├─► Calculate Skill Overlap
   │   ├─► Matched: {Python, JS}
   │   ├─► Missing: {AWS, Docker}
   │   └─► Skill match rate: 50%
   │
   ├─► TF-IDF Vectorization
   │   ├─► Convert texts to vectors
   │   ├─► Weight by term frequency
   │   └─► Consider document frequency
   │
   ├─► Cosine Similarity
   │   └─► cos(θ) = (A·B) / (||A|| ||B||)
   │
   └─► Calculate Weighted Score
       └─► 40% skill + 30% tech + 30% similarity

4. AI Feedback (ai_feedback.py)
   │
   ├─► Construct Prompt
   │   ├─► Include resume text
   │   ├─► Include extracted skills
   │   ├─► Include job description
   │   └─► Include missing skills
   │
   ├─► Send to AI Provider
   │   ├─► If Gemini: use google.generativeai
   │   └─► If OpenAI: use openai.ChatCompletion
   │
   ├─► Parse AI Response
   │   ├─► Extract JSON
   │   ├─► Get improvements list
   │   ├─► Get rewritten bullets
   │   └─► Get summary
   │
   └─► Return Structured Feedback

5. Display Results
   │
   ├─► Show match percentage
   │
   ├─► List matched skills (green)
   │
   ├─► List missing skills (red)
   │
   ├─► Display AI feedback
   │   ├─► ATS improvements
   │   ├─► Rewritten bullets
   │   └─► Recruiter summary
   │
   └─► Scroll to results

Total Time: 1-10 seconds (depending on AI)
```

---

## Authentication Flow

```
┌──────────────────────────────────────────────────────────────────────┐
│                      AUTHENTICATION FLOW                             │
└──────────────────────────────────────────────────────────────────────┘

REGISTRATION:
    User fills form
         │
    Email + Password
         │
    Frontend validation
         │
    POST /api/register
         │
    Backend validates
         │
    Hash password (bcrypt)
         │
    Store in database
         │
    Return user info
         │
    Redirect to login

LOGIN:
    User enters credentials
         │
    POST /api/login
         │
    Backend validates email
         │
    Verify password hash
         │
    Generate JWT token
    {
      "sub": "user@email.com",
      "exp": timestamp + 30min
    }
         │
    Return token
         │
    Store in localStorage
         │
    Redirect to dashboard

PROTECTED REQUESTS:
    User makes request
         │
    Get token from localStorage
         │
    Send in Authorization header
    "Bearer <token>"
         │
    Backend receives request
         │
    Extract token
         │
    Decode & verify JWT
         │
    Check expiration
         │
    Get user from database
         │
    Process request
         │
    Return response

LOGOUT:
    User clicks logout
         │
    Clear localStorage
         │
    Redirect to login
```

---

## Database Schema Relationships

```
┌─────────────────────────────────────────────────────────────────────┐
│                      DATABASE RELATIONSHIPS                          │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────┐
│     USERS       │
├─────────────────┤
│ id (PK)         │◄──────────────────┐
│ email           │                   │
│ password_hash   │                   │ One User
│ created_at      │                   │ Many Resumes
└─────────────────┘                   │
                                      │
                                      │
                    ┌─────────────────┴──────────────────┐
                    │           RESUMES                  │
                    ├────────────────────────────────────┤
                    │ id (PK)                            │
                    │ user_id (FK) ───────────────────┘  │
                    │ filename                           │
                    │ raw_text                           │
                    │ extracted_skills (JSON)            │
                    │ technical_skills (JSON)            │
                    │ soft_skills (JSON)                 │
                    │ tools (JSON)                       │
                    │ ats_score                          │
                    │ created_at                         │
                    └────────────────┬───────────────────┘
                                    │
                        One Resume  │
                        Many Matches│
                                    │
                    ┌───────────────┴────────────────────┐
                    │         JOB_MATCHES                │
                    ├────────────────────────────────────┤
                    │ id (PK)                            │
                    │ resume_id (FK) ──────────────────┘ │
                    │ job_id (FK) ──────────┐            │
                    │ match_percentage      │            │
                    │ missing_skills (JSON) │            │
                    │ ai_feedback           │            │
                    │ created_at            │            │
                    └───────────────────────┼────────────┘
                                           │
                                           │
                          ┌────────────────┴─────────┐
                          │        JOBS              │
                          ├──────────────────────────┤
                          │ id (PK)                  │
                          │ title                    │
                          │ description              │
                          │ required_skills (JSON)   │
                          │ created_at               │
                          └──────────────────────────┘
```

---

## Technology Stack Layers

```
┌─────────────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                                │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────────────┐   │
│  │     HTML5     │  │     CSS3      │  │   JavaScript ES6+     │   │
│  │  - Semantic   │  │  - Variables  │  │   - Async/Await       │   │
│  │  - Forms      │  │  - Flexbox    │  │   - Fetch API         │   │
│  │  - Canvas     │  │  - Grid       │  │   - DOM Manipulation  │   │
│  └───────────────┘  │  - Responsive │  │   - Chart.js          │   │
│                     └───────────────┘  └───────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                             │
                        HTTP/JSON
                             │
┌─────────────────────────────────────────────────────────────────────┐
│                    APPLICATION LAYER                                 │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │                        FastAPI                              │    │
│  │  - REST API endpoints                                       │    │
│  │  - Request validation (Pydantic)                            │    │
│  │  - Response serialization                                   │    │
│  │  - CORS middleware                                          │    │
│  │  - JWT authentication                                       │    │
│  └─────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────┘
                             │
┌─────────────────────────────────────────────────────────────────────┐
│                    BUSINESS LOGIC LAYER                              │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐    │
│  │ PDF Parser │  │    NLP     │  │ ATS Scorer │  │  Matcher   │    │
│  │pdfplumber  │  │   spaCy    │  │  Custom    │  │  TF-IDF    │    │
│  │ PyMuPDF    │  │            │  │ Algorithm  │  │  Cosine    │    │
│  └────────────┘  └────────────┘  └────────────┘  └────────────┘    │
└─────────────────────────────────────────────────────────────────────┘
                             │
┌─────────────────────────────────────────────────────────────────────┐
│                    DATA ACCESS LAYER                                 │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │                     SQLAlchemy ORM                          │    │
│  │  - Model definitions                                        │    │
│  │  - Relationship management                                  │    │
│  │  - Query builder                                            │    │
│  │  - Session management                                       │    │
│  └─────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────┘
                             │
┌─────────────────────────────────────────────────────────────────────┐
│                    PERSISTENCE LAYER                                 │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │                    SQLite Database                          │    │
│  │  (or PostgreSQL for production)                             │    │
│  └─────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────┘

                    External Services
        ┌──────────────────┬──────────────────┐
        │                  │                  │
   ┌────▼─────┐     ┌──────▼──────┐   ┌──────▼──────┐
   │ OpenAI   │     │   Gemini    │   │   spaCy     │
   │   API    │     │     API     │   │   Models    │
   └──────────┘     └─────────────┘   └─────────────┘
```

---

**This architecture ensures:**
- ✅ Separation of concerns
- ✅ Scalability
- ✅ Maintainability
- ✅ Testability
- ✅ Security
- ✅ Performance

**Every component is production-ready!** 🚀
